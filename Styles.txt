/* ==========================================================================
   1. Design Variables & Global Reset
   ========================================================================== */
:root {
    --bg-body: #f8fafc;
    --bg-card: #ffffff;
    --primary: #0f766e;          /* Your beautiful deep teal */
    --primary-hover: #115e59;    /* Darker teal for hover */
    --text-main: #1e293b;        /* Dark slate for high readability */
    --text-muted: #64748b;      /* Soft gray for subheadings/captions */
    --border-color: #e2e8f0;    /* Light clean border separator */
    --radius: 10px;              /* Consistent rounded corners */
    --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05), 0 2px 4px -2px rgba(0, 0, 0, 0.05);
    --transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
}

* {
    box-sizing: border-box;
}

body {
    margin: 0;
    font-family: 'Segoe UI', system-ui, -apple-system, sans-serif;
    color: var(--text-main);
    background-color: var(--bg-body);
    line-height: 1.6;
    -webkit-font-smoothing: antialiased;
}

/* ==========================================================================
   2. Modern Header & Navigation
   ========================================================================== */
header {
    background-color: #1f2937; /* Dark charcoal */
    color: #ffffff;
    padding: 40px 24px;
    text-align: center;
}

header h1 {
    font-size: 2.2rem;
    font-weight: 800;
    letter-spacing: -0.025em;
    margin: 0 0 12px 0;
}

header p {
    color: #94a3b8;
    max-width: 650px;
    margin: 0 auto 24px auto;
    font-size: 1.05rem;
}

nav {
    display: flex;
    justify-content: center;
    gap: 15px;
    flex-wrap: wrap;
}

nav a {
    color: #cbd5e1;
    font-weight: 600;
    text-decoration: none;
    padding: 8px 16px;
    border-radius: 6px;
    transition: var(--transition);
}

nav a:hover {
    color: #ffffff;
    background-color: rgba(255, 255, 255, 0.1);
    text-decoration: none;
}

/* ==========================================================================
   3. Main Container & Elegant Section Cards
   ========================================================================== */
main {
    max-width: 850px;
    margin: 40px auto;
    padding: 0 24px;
}

section {
    background-color: var(--bg-card);
    margin-bottom: 30px;
    padding: 35px;
    border-radius: var(--radius);
    border: 1px solid var(--border-color);
    box-shadow: var(--shadow);
}

h2 {
    font-size: 1.5rem;
    font-weight: 700;
    margin-top: 0;
    margin-bottom: 16px;
    color: #0f172a;
    display: flex;
    align-items: center;
    gap: 10px;
}

/* Adds a subtle primary-colored indicator next to each section title */
h2::before {
    content: '';
    display: inline-block;
    width: 4px;
    height: 20px;
    background-color: var(--primary);
    border-radius: 4px;
}

p {
    color: var(--text-muted);
    margin-top: 0;
    margin-bottom: 20px;
}

/* Custom list styling */
ul, ol {
    padding-left: 20px;
    margin: 0;
}

li {
    margin-bottom: 12px;
    color: var(--text-muted);
}

li::marker {
    color: var(--primary);
}

/* ==========================================================================
   4. Connect links list conversion (Card Grid Layout)
   ========================================================================== */
#links ul {
    list-style: none;
    padding: 0;
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(180px, 1fr));
    gap: 12px;
    margin-top: 20px;
}

#links a {
    display: block;
    padding: 14px;
    background-color: #f1f5f9;
    color: var(--primary);
    text-decoration: none;
    font-weight: 600;
    text-align: center;
    border-radius: var(--radius);
    transition: var(--transition);
    border: 1px solid var(--border-color);
}

#links a:hover {
    background-color: var(--primary);
    color: #ffffff;
    transform: translateY(-2px);
    box-shadow: 0 4px 12px rgba(15, 118, 110, 0.15);
}

/* ==========================================================================
   5. Photo Gallery Grid Layout
   ========================================================================== */
.gallery-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 20px;
    margin-top: 24px;
}

.gallery-grid img {
    width: 100%;
    height: 260px;
    object-fit: cover;
    border-radius: var(--radius);
    box-shadow: var(--shadow);
    transition: var(--transition);
}

.gallery-grid img:hover {
    transform: scale(1.03);
    box-shadow: 0 12px 20px -8px rgba(0, 0, 0, 0.15);
}

/* ==========================================================================
   6. Polished Forms & Interactive UI Controls
   ========================================================================== */
form {
    margin-bottom: 0;
}

.form-group, form > div {
    display: flex;
    flex-direction: column;
    gap: 6px;
    margin-bottom: 20px;
}

label {
    font-weight: 600;
    font-size: 0.9rem;
    color: var(--text-main);
}

input, select, textarea, button {
    font: inherit;
}

/* Standard inputs basic styling */
input[type="text"],
input[type="email"],
select,
textarea {
    width: 100%;
    padding: 12px 16px;
    border: 1px solid var(--border-color);
    background-color: #ffffff;
    border-radius: 8px;
    color: var(--text-main);
    transition: var(--transition);
}

/* Form element focus outlines */
input:focus, select:focus, textarea:focus {
    outline: none;
    border-color: var(--primary);
    box-shadow: 0 0 0 3px rgba(15, 118, 110, 0.15);
}

/* Textarea handling */
textarea {
    resize: vertical;
    min-height: 120px;
}

/* Styling native browser details inside dropdowns */
select {
    cursor: pointer;
    appearance: none;
    background-image: url("data:image/svg+xml;charset=UTF-8,%3csvg xmlns='http://www.w3.org/2000/svg' viewBox='0 0 24 24' fill='none' stroke='%23475569' stroke-width='2' stroke-linecap='round' stroke-linejoin='round'%3e%3cpolyline points='6 9 12 15 18 9'%3e%3c/polyline%3e%3c/svg%3e");
    background-repeat: no-repeat;
    background-position: right 16px center;
    background-size: 16px;
    padding-right: 40px;
}

/* Clean Fieldset System */
fieldset {
    border: 1px solid var(--border-color);
    border-radius: var(--radius);
    padding: 20px;
    margin-bottom: 24px;
    display: flex;
    flex-direction: column;
    gap: 12px;
}

legend {
    font-weight: 700;
    font-size: 0.85rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--primary);
    padding: 0 8px;
}

/* Checkboxes and radio container aligner */
.option-group, fieldset label {
    display: flex;
    align-items: center;
    gap: 10px;
    font-weight: normal;
    cursor: pointer;
    color: var(--text-main);
    margin-bottom: 0;
}

.option-group input[type="radio"],
.option-group input[type="checkbox"],
fieldset label input {
    width: 18px;
    height: 18px;
    cursor: pointer;
    accent-color: var(--primary); /* Keeps color palette consistent */
}

/* Interactive Submit Button styling */
button, button[type="submit"] {
    background-color: var(--primary);
    color: #ffffff;
    border: none;
    padding: 14px 28px;
    font-size: 1rem;
    font-weight: 600;
    border-radius: 8px;
    cursor: pointer;
    transition: var(--transition);
    align-self: flex-start; /* Keeps the button sized to fit text on wide screens */
}

button:hover, button[type="submit"]:hover {
    background-color: var(--primary-hover);
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(15, 118, 110, 0.2);
}

button:active, button[type="submit"]:active {
    transform: translateY(0);
}

/* ==========================================================================
   7. Footer Area
   ========================================================================== */
footer {
    text-align: center;
    padding: 40px 24px;
    background-color: #1f2937;
    border-top: 1px solid var(--border-color);
    color: #94a3b8;
    font-size: 0.9rem;
}